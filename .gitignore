.build/
.DS_Store
DerivedData/
*.xcuserstate
xcuserdata/
__pycache__/
*.pyc

# Android signing material must never be committed. Release signing is supplied
# to GitHub Actions through repository secrets.
*.jks
*.keystore
*.p12
android/keystore.properties

# Firebase Android configuration is fetched from Firebase Management API by
# configured CI/release workflows and must not be committed to the public repo.
android/app/google-services.json
